# 编撰框指令包 · F-09 触发词汇总表生产
> **指令版本**：v1.0
> **建立时间**：2026-04-26
> **下发方**：中枢-智核中枢·iS-Core
> **执行方**：编撰-智核中枢·iS-Core（编撰框）
> **数据源仓库**：`carefulxiao-cell/Synexa-iS-OIOS`
> **状态**：🔵 生产中

---

## 一、任务元数据与目标 (TBF)

- **任务代号**：F-09
- **交付物**：`TRIGGER_GLOSSARY_v1.0.md`
- **存放目录**：`00-master/`
- **核心目标**：汇总 Synexa iS 体系全局触发词及其响应逻辑，形成 SSOT（单点真实源），供全局枢纽系统「触发词汇总」板块读取与展示。

---

## 二、必须读取的基线文件

在开始生产前，编撰框**必须**读取以下文件以获取最新全局状态与触发词定义：
1. `02-context-os/CONTEXT_OS.md`（全局状态总账本，关注全局机制注册中的触发词）
2. `00-master/WORKSPACE_REGISTRY_v2.1.md`（对话组全局注册表，关注各组/框的触发词字段）
3. `00-master/RACI_v1.0.md`（角色责任表，关注各角色对应的动作权限）
4. `00-master/ISYNC_PROTOCOL.md`（全局指令同步协议，关注 ISYNC 相关的三个核心触发词及 SOP）
5. `00-master/W01_INSTRUCTION_PACKAGE_v1.5.md`（执行框指令包，了解系统端对触发词数据的读取需求）
6. `00-master/COMPILE_INSTRUCTION_PACKAGE_v1.2.md`（编撰框指令包，了解编撰规范与边界）

---

## 三、内容结构要求

`TRIGGER_GLOSSARY_v1.0.md` 必须包含以下核心模块，并严格按此结构组织：

### 1. 文件头 (Header)
包含版本号（v1.0）、更新时间、定位（全局触发词与响应逻辑的唯一真实源）、维护者（中枢-智核中枢·iS-Core）。

### 2. 触发词分类矩阵 (Trigger Matrix)
将所有收集到的触发词按**应用场景/机制**进行分类，建议分为以下几类：
- **全局同步与审查类**（如 `汇总指令`、`全面审查 [项目代号]`、`汇总推送 [项目代号]`）
- **基线与状态确认类**（如 `读取基线`）
- **项目立项与启动类**（如 `生成启动语`）
- **其他特定机制类**（根据基线文件提取）

### 3. 触发词详细定义表 (Detailed Definitions)
使用 Markdown 表格呈现，必须包含以下字段：
- **触发词 (Trigger Phrase)**：精确的触发词文本（如 `全面审查 [项目代号]`）。
- **所属对话组/框 (Applicable Workspace/Frame)**：该触发词在哪些组或框中生效（如“所有接入 ISYNC 的工作空间”、“中枢-智核中枢·iS-Core”）。
- **触发动作 (Trigger Action)**：用户输入该词的意图。
- **响应逻辑 (Response Logic)**：Agent 收到该词后必须执行的标准序列（SOP）摘要。
- **权限要求 (Permission/Role)**：谁有权触发，谁负责执行（参考 RACI 表）。

---

## 四、边界与禁忌

1. **只汇总已定义的触发词**：严禁自行发明、创造或推测基线文件中未明确定义的触发词。所有触发词必须有明确的出处（如 ISYNC 协议、注册表等）。
2. **不改变原有响应逻辑**：在描述「响应逻辑」时，必须忠实于原协议（如 ISYNC_PROTOCOL）的定义，进行精炼摘要，不得擅自修改执行步骤。
3. **禁止自行宣布任务关闭**：编撰框完成初稿后，必须将完整文件内容输出至对话，等待「中枢-智核中枢·iS-Core」审核。**严禁自行宣布任务完成、终稿确认或自行推送 GitHub。**

---

## 五、交付标准与回报流程

1. **格式要求**：纯 Markdown 格式，表格结构清晰，无 HTML 标签。
2. **回报方式**：将 `TRIGGER_GLOSSARY_v1.0.md` 的**完整内容**直接输出在对话中，供用户带回中枢框审核。
3. **入库流程**：中枢框审核通过后，由 iS-Core 统一推送至 GitHub `00-master/` 目录，并更新 `EXECUTION_TRACKER.md`。

---
*本指令包由中枢-智核中枢·iS-Core 生成，请用户复制本全文，发送给【编撰-智核中枢·iS-Core】执行。*
