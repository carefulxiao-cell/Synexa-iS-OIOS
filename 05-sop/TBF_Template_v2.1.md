# [项目代号] [版本号] 任务目标基线文件（TBF）

> **版本**：v2.1 (基于 ISYNC 协议 v1.1 升级，引入三态核查与强制门禁)
> **指令包文件**：`[对应的指令包文件名].md`
> **创建时间**：YYYY-MM-DD
> **最后核查时间**：YYYY-MM-DD

---

## 一、功能性需求基线 (Functional Requirements)

> **说明**：列出本次迭代必须实现的所有业务功能点。执行框开发完成后，必须逐项打钩并填写验证方式。
> **注意**：验证方式必须包含**生产环境的实际操作或状态截图**，不能仅依赖代码测试通过。

### 模块一：[模块名称]
- [ ] 功能点 1 | 验证方式：[如：生产环境操作截图/生产数据库 SQL 查询结果] | 状态：待开发
- [ ] 功能点 2 | 验证方式：[如：生产环境操作截图/生产数据库 SQL 查询结果] | 状态：待开发

### 模块二：[模块名称]
- [ ] 功能点 3 | 验证方式：[如：生产环境操作截图/生产数据库 SQL 查询结果] | 状态：待开发

---

## 二、非功能性需求基线 (NFR - Non-Functional Requirements)

> **说明**：列出本次迭代必须满足的性能、安全、兼容性等约束条件。

- [ ] **性能**：[如：核心页面加载时间 < 2秒 / API 响应时间 < 500ms] | 验证方式：[如：Network 面板截图] | 状态：待开发
- [ ] **安全**：[如：所有新增 API 必须经过鉴权中间件 / 敏感数据必须脱敏] | 验证方式：[如：代码走查] | 状态：待开发
- [ ] **兼容性**：[如：必须适配移动端浏览器 / 必须兼容旧版数据结构] | 验证方式：[如：移动端截图] | 状态：待开发
- [ ] **可维护性**：[如：新增代码必须包含关键注释 / 必须编写对应的单元测试] | 验证方式：[如：测试覆盖率报告] | 状态：待开发

---

## 三、架构设计确认 (Architecture Validation)

> **说明**：在编写核心代码前，执行框必须先输出架构设计并在此处登记，由中枢框确认后方可编码。

- [ ] **数据库变更**：[如：新增表结构定义 / 字段修改说明] | 确认状态：待确认
- [ ] **API 接口定义**：[如：新增路由及出入参格式] | 确认状态：待确认
- [ ] **核心依赖**：[如：引入了哪些新的第三方库及其版本] | 确认状态：待确认

---

## 四、执行框回写记录（强制提交规范）

> **说明**：执行框在宣告交付前，必须严格按照 `SOP_003_Execution_Submission_v1.0.md` 的规范填写提交报告。不符合格式的提交，PCS 有权拒绝接收。

**[YYYY-MM-DD] 交付自测记录**：

*   **Commit Hash**：[填写]
*   **执行的指令包**：[文件名]
*   **完成项**：[列表]
*   **未完成项**：[列表，含原因]
*   **架构规范自查**：
    - [ ] 无原生 SQL（如 `mysql2/promise` 或 `conn.execute`）
    - [ ] 无独立建表脚本（所有变更均通过 Drizzle ORM 迁移）
    - [ ] `schema.ts` 与数据库实际结构一致（**必须附生产数据库 `SHOW COLUMNS` 截图或输出**）
*   **生产环境验证**：[截图或详细说明]

---

## 五、审查框/中枢框核查结论（三态门禁）

> **说明**：由审查 Agent 或中枢框在执行 `全面审查` 后填写。必须根据三态定义给出明确结论。

**[YYYY-MM-DD] 独立审查记录**：

*   **已确认完成**：X 项
*   **部分完成 (🟡)**：X 项（列出明细）
*   **未落地 (❌)**：X 项（列出明细）

**最终 TBF 状态裁决**：
- [ ] ✅ **TBF 通过**（代码提交 + 生产环境端到端验证通过，解锁下一阶段）
- [ ] ⚠️ **TBF 待验证**（代码已提交，生产环境未验证，**阻断下一阶段**）
- [ ] ❌ **TBF 未通过**（生产环境验证失败，**打回修复**）

**下一步行动**：[如：准予发布 / 驳回修复明细 / 补充生产验证]
