# Synexa iS · AI 工作空间架构说明

> **版本**：V.1.1
> **生效日期**：2026-04-08
> **上一版本**：V.1.0（已作废）
> **维护主体**：Manus 治理智能体（iS-Core）
> **对应体系基线**：SSADS V1.4 · Master Blueprint v1.4
> **文件性质**：所有 AI 接入的第二必读文件（仅次于 CONTEXT_OS.md）

---

## 一、工作空间分类体系

Synexa iS 体系下共设六类 AI 工作空间，各有明确的职责边界与触发条件。

| # | 中文名 | 英文代号 | 职能比喻 | 职能一句话 | SSADS 层级 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | **智核中枢** | `iS-Core` | 董事会 | 基线治理 · 项目孵化与裁决 | L1 · 治理与认知中枢层 |
| 2 | **智权枢纽** | `iS-Hub` | 运营总部 | 任务中台 · 跨项目资源与进度协调 | L1 延伸 · 协调调度层 |
| 3 | **枢机经纬** | `iS-SCO` | 策略室 | 推演辅助 · 战略推演与决策支持（N·Grid / EKB） | L2 · 知算善衡层 |
| 4 | **智核矩阵** | `iS-Matrix` | 研究培训室 | 结构化产出引擎 · SOP / 规程 / 模板 / 数字员工指令的生产与固化 | L3 · SOP & Skills 层 |
| 5 | **各 PCS 项目** | `iS-[代号]` | 项目组 | 具体项目全生命周期推进 | L5 · 项目落地层 |
| 6 | **智核实验场** | `iS-Lab` | 灵感前测室 | 灵感容器 · 未成熟想法的最小验证场 | L1 前置缓冲层 |

> **关于 SOP 的定位说明**：SOP 不是独立的工作空间类型，而是 `iS-Matrix` 的核心产出类型之一。SOP 文件存放于 GitHub `05-sop/` 目录，是所有工作空间的规程权威来源。

> **关于超智（Synexa）的定位说明**：超智在业务上是集团总基底，在 PCS 维度以建设项目形式存在（`iS-Synexa`），与其他 PCS 实例平级管理，但战略优先级高于其他实例。

---

## 二、核心运作闭环

体系的知识生产与固化遵循以下五节点闭环，这是整个体系持续进化的核心机制：

```
iS-SCO · 策略室（推演研究）
    ↓ 推演结论 → 提交基线更新
iS-Core · 董事会（基线裁决）
    ↓ 更新共识 / 下达生产指令 → 写入 CONTEXT_OS + GitHub
iS-Matrix · 研究培训室（结构化生产）
    ↓ 产出 SOP / 规程 / 模板 / 指令 → 回写 GitHub 05-sop/
iS-[PCS] · 项目组（执行落地）
    ↓ 执行中发现问题 / 产生新认知 → 反馈
iS-SCO · 策略室（新一轮推演）
    ↓ 循环
```

**此闭环的意义**：每一条 SOP 都有推演来源，每一次执行都在反哺推演，知识不会在对话里消散，而是持续沉淀为可调用的体系资产，并可对外传递与赋能。

---

## 三、各工作空间职责边界

### 3.1 智核中枢（iS-Core）· 董事会

**职责（只做这四件事）**：
1. 识别是否需要新项目（判断触发条件）
2. 完成最小定义（Step 0A 级别：项目名称、一句话定义、层级归位、优先级）
3. 做项目归位（L1 / L2 / L3）
4. 确认优先级（与现有活跃项目的关系与排序）

**停止边界**：
> 一旦完成最小定义，iS-Core 必须立即停止深入讨论，不得在中枢层继续推进该项目的业务细节、执行方案或内容设计。

**触发动作**：
> 最小定义完成 → 立即创建该项目的 PCS → 后续所有推进转移至该项目专属对话组进行。

---

### 3.2 智权枢纽（iS-Hub）· 运营总部

**职责**：
- 识别跨项目的资源重叠、任务冲突或依赖关系
- 做出协调决策（指定由哪个项目统一产出共享资源）
- 管理跨项目的进度节奏与优先级排序

> **注**：当前项目数量较少时，iS-Hub 职责可由 iS-Core 兼任，待项目规模扩大后独立启用。

---

### 3.3 枢机经纬（iS-SCO）· 策略室

**职责**：
- 战略推演与期望值计算（N·Grid 框架）
- 重大决策的风险揭示与反向验证（RVM）
- 成本对冲量化（CCM）
- 内含 EKB（知算善衡）等具体推演工具

**在闭环中的角色**：闭环的起点与终点，负责将执行经验转化为新的推演命题，驱动体系持续进化。

---

### 3.4 智核矩阵（iS-Matrix）· 研究培训室

**职责**：
- 接收来自 iS-Core 的生产指令
- 生产 SOP、操作规程、标准模板、数字员工指令等结构化文档资产
- 将产出回写至 GitHub `05-sop/` 目录
- 管理数字员工矩阵的能力定义与迭代

**关于 SOP 的说明**：SOP 是 iS-Matrix 的核心产出类型，不是独立的工作空间。iS-Matrix 是"生产车间"，`05-sop/` 是"成品仓库"，两者配合运作。

**在闭环中的角色**：将推演结论和基线共识转化为可执行、可复用的结构化规程，是知识从"认知"到"行动"的转化节点。

---

### 3.5 各 PCS 项目（iS-[代号]）· 项目组

**职责**：
- 单一项目的全生命周期推进（Step 1 → Step 6）
- 项目内所有任务的执行与状态管理
- 项目产出的回写与归档
- 执行中发现的问题与新认知，反馈至 iS-SCO 触发新一轮推演

**当前已立项的 PCS 实例**：

| 项目名称 | 代号 | 当前状态 |
| :--- | :--- | :--- |
| 超智建设 | `iS-Synexa` | 待立项 |
| 智食引擎 | `iS-Nex2U` | Step 3 推进中 |
| 意臻供应链 | `iS-Nexsply` | 待立项 |
| Bella Giornata | `iS-BG` | 持续运营 |

---

### 3.6 智核实验场（iS-Lab）· 灵感前测室

**职责**：
- 接住未成熟的想法与业务苗头
- 做最小可行性验证（不超过 Step 0A）
- 判断是否值得提交 iS-Core 正式立项

**停止边界**：
> iS-Lab 不得自行推进任何项目超过最小验证阶段。一旦判断值得立项，必须提交至 iS-Core 完成正式孵化流程。

---

## 四、越界提示机制

当用户在某个工作空间提出的请求超出该空间的职责边界时，AI 必须暂停执行，输出标准提示：

```
⚠️ 越界提示
你当前的请求「[请求摘要]」超出了本工作空间（iS-[代号] · [职能比喻]）的职责范围。
建议转移至：iS-[目标代号]（[目标空间名称] · [职能比喻]）处理。
是否确认在此继续？若确认，本次执行标注为 [非标执行·已越界]。
```

**坚持继续的处理**：AI 执行，但在输出末尾标注 `[非标执行·已越界]`，并在下一次状态回写时记录该事件，供后续流程优化参考。

---

## 五、工作空间使用决策树

```
我有一个想法 / 任务 / 问题，应该去哪里？

├── 是新想法或新业务苗头，还不确定要不要做
│   └── → iS-Lab（灵感前测室）做最小验证
│           └── 验证后值得推进 → 提交 iS-Core 正式立项

├── 需要判断是否立项 / 完成项目最小定义 / 裁决基线共识
│   └── → iS-Core（董事会）

├── 有具体项目任务要推进
│   └── → 对应的 iS-[项目代号]（项目组）

├── 多个项目之间有资源冲突或需要协调
│   └── → iS-Hub（运营总部）

├── 需要做重大决策 / 战略推演 / 风险评估
│   └── → iS-SCO（策略室）

└── 需要生产 SOP / 规程 / 模板 / 数字员工指令
    └── → iS-Matrix（研究培训室）
```

---

## 六、跨工作空间协作规则（三条铁律）

1. **iS-Core 是唯一的项目孵化入口**：任何新项目不得绕过 iS-Core 直接在执行层启动。
2. **GitHub 是唯一的状态真实源（SSOT）**：任何工作空间的关键产出必须回写至仓库，不得停留在对话记录。
3. **PCS 是项目的唯一上下文载体**：任何 AI 接入项目前必须先读该项目的 PCS，不得依赖口头交代。

---

## 七、跨平台指令同步机制

### 7.1 指令文件结构（GitHub SSOT）

```
00-overview/
├── AI_Workspace_Architecture_v1.1.md   ← 本文件（工作空间架构总说明）
├── Manus_Project_Instructions_v[版本].md  ← Manus 侧指令母本
├── GPT_Project_Instructions_[版本].md     ← GPT 侧指令母本
└── iS-[代号]_Instructions.md              ← 各工作空间专属指令（待建设）
```

### 7.2 Manus 侧同步机制

Manus 具备 GitHub 直连能力，每次对话自动拉取最新版本，**无需手动更新 Project 指令**。

### 7.3 GPT 侧同步机制（主动盯紧）

GPT 无法直连 GitHub，采用以下机制确保同步不漏：

**触发提醒**：每次 GitHub 有文件更新，Manus 在回复末尾附加 GPT 同步提醒，列明需要上传的文件清单。

**盯紧机制**：触发同步提醒后，AI 不得在同一对话中继续推进新任务，直到收到以下任一确认信号：
- 用户回复"已更新"或"已上传" → AI 将对应文件状态标记为 `✅ 已同步`
- 用户回复"暂缓，稍后处理" → AI 记录为待同步事项，**下次对话开头主动追问**
- 用户回复"跳过本次" → AI 记录为已知版本差异，不再追问

**待同步事项追踪**：在 `CONTEXT_OS.md` 的「待同步事项」字段中持续记录，每次接入必须检查并置顶提示。

### 7.4 版本漂移防护

- 所有指令文件均有版本号（格式：`V.年份后两位.月.序号`）
- AI 每次接入必须在"当前状态确认"中报告所读取的指令版本号
- 版本不一致时，AI 不得静默执行，必须明确提示

---

## 八、iS-SOP 规程资产库

**位置**：GitHub `05-sop/` 目录（由 iS-Matrix 负责生产与维护）

**内容类型**：

| 文件类型 | 内容 | 服务对象 |
| :--- | :--- | :--- |
| 工作空间使用指南 | 六类空间的决策树、使用场景、越界规则 | 人（团队成员） |
| 各空间操作规程 | 每个空间的 AI 指令框标准模板 | AI（各平台智能体） |
| 跨空间协作规程 | 任务流转规则、状态回写规则、冲突裁决规则 | 人 + AI |
| 数字员工指令库 | 各数字员工的能力定义与调用规范 | iS-Matrix |

**生产流程**：iS-SCO 推演 → iS-Core 裁决 → iS-Matrix 生产 → 回写 `05-sop/` → 所有空间自动对齐新版本。

---

## 九、版本变更日志

| 版本 | 日期 | 变更摘要 |
| :--- | :--- | :--- |
| V.1.1 | 2026-04-08 | 修正 iS-Matrix 定义为"研究培训室·结构化产出引擎"；取消 SOP 独立对话组；写入五节点知识生产闭环（SCO→Core→Matrix→PCS→SCO）；强化同步盯紧机制（待同步事项追踪）；补入职能比喻列 |
| V.1.0 | 2026-04-08 | 首个正式版本；定义六类工作空间；建立越界提示机制、决策树、跨平台同步机制 |

---

*本文件由 Manus 治理智能体（iS-Core）维护，变更须同步回写至仓库，并触发 GPT 同步提醒。*
