# NexFlow V3.1 任务目标基线文件（TBF）

> **版本**：V3.1
> **指令包文件**：`nexflow_v3_1_health_dashboard.md`
> **创建时间**：2026-04-16
> **最后核查时间**：待核查

---

## 一、核查清单

### 模块：健康看板（/health-dashboard）

- [ ] `HealthDashboard.tsx` 页面存在，含顶部统计卡片和三个预警 Tab | 验证：截图确认 | 状态：待核查
- [ ] 侧边栏新增「健康看板」导航项 | 验证：截图确认 | 状态：待核查
- [ ] `App.tsx` 注册 `/health-dashboard` 路由 | 验证：代码检查 | 状态：待核查
- [ ] `server/routers/healthDashboard.ts` 存在且注册至 `routers.ts` | 验证：代码检查 | 状态：待核查
- [ ] `getStats` 接口实现并返回三大预警数量 | 验证：代码检查 | 状态：待核查
- [ ] `getCertificateAlerts` 接口实现，基于 `hr_certificates` 查询过期/临期证件 | 验证：代码检查 | 状态：待核查
- [ ] `getInspectionAlerts` 接口实现，基于 `ops_inspections` 查询未解决/超期巡检 | 验证：代码检查 | 状态：待核查
- [ ] `getTrainingAlerts` 接口实现，基于 `hr_trainings` 查询未完成培训 | 验证：代码检查 | 状态：待核查
- [ ] 证件预警列表：红/橙色状态标签显示正确 | 验证：截图确认 | 状态：待核查
- [ ] 巡检跟进列表：红/橙色状态标签显示正确 | 验证：截图确认 | 状态：待核查
- [ ] 培训监控列表：红/橙色状态标签显示正确 | 验证：截图确认 | 状态：待核查
- [ ] 全量测试通过（TypeScript 编译无误） | 验证：`pnpm typecheck` | 状态：待核查

---

## 二、执行框回写记录

*(执行框完成后，在此填写实际完成情况和遗漏说明)*

---

## 三、SCO 核查结论

**已确认完成**：0项
**确认未完成**：0项
**待执行框确认**：12项

**核查结论**：待执行框执行 V3.1 指令包后进行首次核查。
